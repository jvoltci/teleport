# Listen port
PORT=4000

# Comma-separated list of allowed CORS origins. In production, set this to your
# frontend's URL — e.g., https://your-username.github.io
CORS_ORIGINS=*

# Seconds an inactive room is kept before cleanup
ROOM_TTL_SECONDS=60

# Logging
LOG_LEVEL=INFO
